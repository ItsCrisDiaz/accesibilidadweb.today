---
import { Module8Lessons } from "../../../data/Module8Lessons";
// Component Imports
import Layout from "../../../layouts/layout.astro";
import ModuleHero from "../../../components/ModuleHero/ModuleHero.astro";
---

<Layout
  theme="8"
  title="Diseño de interfaces - Accesibilidadweb.today"
  description="Aprende sobre como crear proyectos accesibles desde la fase de UX y diseño de interfaces"
>
  <ModuleHero module="8">
    <h1>Diseño de interfaces</h1>
    <p class="fs-500">
      Aprende como implementar la creación de productos accesibles desde la fase
      de UX y diseño de interfaces aquí.
    </p>
  </ModuleHero>
  <section aria-labelledby="lessons-title">
    <div class="wrapper flow" data-width="narrow">
      <h2 id="lessons-title">Lecciones</h2>
      <ul>
        {
          Module8Lessons.map((element) => {
            return (
              <li>
                <p>
                  <a
                    href={element.url ? element.url : null}
                    set:html={element.title}
                  />{" "}
                  {element.url ? null : "- (En proceso)"}
                </p>
              </li>
            );
          })
        }
      </ul>
      <p class="fs-400">
        ¿Tienes alguna sugerencia sobre qué se puede enseñar en este o en
        cualquier otro módulo? ¡No dudes en enviar tu sugerencia a <a
          class="text-wrap"
          href="mailto:cristian@accesibilidadweb.today"
          ><b>cristian@accesibilidadweb.today</b></a
        >!
      </p>
    </div>
  </section>
</Layout>
